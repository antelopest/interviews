/*
Дженерик-функция фильтрации

Задача:
Напишите типизированную функцию filterArray, 
которая принимает массив элементов любого типа T и функцию-предикат,
возвращая отфильтрованный массив того же типа T.
*/

function filterArray<T>(arr: T[], predicate: (item: T) => boolean): T[] {
  // Ваша реализация
}